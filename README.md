# MiSTer-tools

## Installation

1. Add the following to `/media/fat/downloader.ini`

```ini
[davewongillies/mister-tools]
db_url = https://raw.githubusercontent.com/davewongillies/MiSTer-tools/db/db.json.zip
```

2. Run `update_all` from the `Scripts` menu
